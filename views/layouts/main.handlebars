<!DOCTYPE html>

<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <header>
    <div>
      <h1 class="p-3 mb-3 font-monospace">Tech Blog</h1>
      {{#if logged_in}}
        <h3 class="ms-4 font-monospace" id="welcome-header">Welcome back {{logged_in_username}}</h3>
      {{/if}}
    </div>
    <div>
      <nav class="nav">
        <a class="nav-link" href="/">My Home</a>
        <a class="nav-link" href="/dashboard">My Dashboard</a>
        {{#if logged_in}}
          <button type="button" class="btn btn-link nav-link" id="logout">Logout</button>
        {{else}}
          <a class="nav-link" href="/login">Login</a>
        {{/if}}
      </nav>
    </div>
  </header>

  <main>
    {{{body}}}
  </main>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  {{#if logged_in}}
    <script src="/js/logout.js"></script>
  {{/if}}

</body>

</html>